<!DOCTYPE html>
<html>
  <head>
    <title>Download <%= track.name %></title>
  </head>
  <body>
    <h1><%= track.name %></h1>
    <p>Artist: <%= track.artists[0].name %></p>
    <img src="<%= track.album.images[0].url %>" width="300" />
    <p>Album: <%= track.album.name %></p>
    <p><a href="<%= youtubeUrl %>" target="_blank">Watch on YouTube</a></p>
    <a
      href="/download-mp3?youtubeUrl=<%= youtubeUrl %>&title=<%= encodeURIComponent(track.name + ' - ' + track.artists[0].name) %>"
    >
      <button>Download MP3</button>
    </a>
  </body>
</html>
